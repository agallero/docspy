<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DocSpy.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DocSpy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Title="D👁️CSpy">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
    <local:SettingsControl x:Name="SettingsPage"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch"
                                />
    <Grid x:Name="ContentPage"
          Visibility="Collapsed"
         ColumnSpacing="10"
            Padding="4, 4, 12,12"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{x:Bind local:Settings.Instance.ViewModel.ButtonPanelWidth, Mode=TwoWay}" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:ServerPanel x:Name="ServerCtrl"
                         Grid.Row="0"
                         Grid.Column="0"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         Margin="0,0,0,0" />

        
        <local:BrowserControl x:Name="BrowserCtrl"
                         Grid.Row="0"
                         Grid.Column="1"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         Margin="0,0,0,0" />

        <controls:GridSplitter Grid.Row="0"
                               Width="8"
                               HorizontalAlignment="Right"
                               ResizeBehavior="BasedOnAlignment"
                               ResizeDirection="Auto">
            <controls:GridSplitter.RenderTransform>
                <TranslateTransform X="+14" />
            </controls:GridSplitter.RenderTransform>
        </controls:GridSplitter>
    </Grid>
    </Grid>

</Window>
