<?xml version="1.0" encoding="utf-8"?>
<Window  
    x:Class="DocSpy.Building"  
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
    xmlns:local="using:DocSpy"  
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
    mc:Ignorable="d"
    Title="Progress...">  

    <Window.SystemBackdrop>  
        <MicaBackdrop />  
    </Window.SystemBackdrop>

    <Grid x:Name="ContentGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
           <TextBlock x:Name="CaptionLabel"   
           VerticalAlignment="Stretch"  
           HorizontalAlignment="Stretch" 
           TextAlignment="Left"
           FontSize="24"
           FontWeight="Bold"
           Margin="10"
           Grid.Row="0"/>
            <TextBlock x:Name="ElapsedTime" Width="150"
                       VerticalAlignment="Bottom"
                       Padding="10"
                       FontSize="18"
                       TextAlignment="Right" HorizontalAlignment="Right" Grid.Row="1"
                       
                       >
            </TextBlock>
        </Grid>
    

    <ProgressBar IsIndeterminate="True" x:Name="ProgressIndicator"  
           VerticalAlignment="Center"  
           HorizontalAlignment="Stretch" 
           Grid.Row="1"/>
        <ScrollView Grid.Row="2" VerticalScrollBarVisibility="Visible" ExtentChanged="ScrollView_ExtentChanged">
            <TextBlock x:Name="LogBox" 
            VerticalAlignment="Stretch"  
            HorizontalAlignment="Stretch" 
            Padding="10"
            IsTextSelectionEnabled="True" 
            TextWrapping="Wrap" />
        </ScrollView>
    </Grid>  
</Window>
