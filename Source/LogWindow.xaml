<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DocSpy.LogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DocSpy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LogWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Text="Log" Style="{ThemeResource TitleTextBlockStyle}" Padding="12" />
        <ScrollView Grid.Row="1" VerticalScrollBarVisibility="Visible" ExtentChanged="ScrollView_ExtentChanged">
            <TextBlock TextWrapping="Wrap" Text="" IsTextSelectionEnabled="True" x:Name="LogBox"  VerticalAlignment="Stretch"  
 HorizontalAlignment="Stretch" 
 Padding="12"/>
        </ScrollView>
        <Button x:Name="ClearButton" Content="Clear" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10" Click="ClearButton_Click"/>
    </Grid>
</Window>
